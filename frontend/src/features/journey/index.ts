// Journey feature, cumulative progress map (no streak anxiety).
// Implemented in task T201. Framing rules: sessions/things-learned,
// never day-count streaks or "missed day" cells (Principle IV).
export {};
